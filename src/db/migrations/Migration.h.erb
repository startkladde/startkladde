#ifndef <%= class_name.upcase %>_H_
#define <%= class_name.upcase %>_H_

#include "src/db/migration/Migration.h"

/**
 * <%= "TO#{}DO" %> migration description
 */
class <%= class_name %>: public Migration
{
	public:
		<%= class_name %> (Db::Interface::Interface &interface);
		virtual ~<%= class_name %> ();

		virtual void up (OperationMonitorInterface monitor=OperationMonitorInterface::null);
		virtual void down (OperationMonitorInterface monitor=OperationMonitorInterface::null);
};

#endif

