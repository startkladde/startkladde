Adding a new permission type to sk_user:
  - add a require_(perm) function in sk_web
  - add a variable in class sk_user and initialize it to the lowest access
	level in the constructor
  - add the attribute in
    - sk_db::row_to_user
	- sk_db::user_value_list
	- sk_db::get_table_information under table_name==user (default to lowest
	  access level)
	- sk_db::sk_user_list
  - Update the database, for example, using sk_admin init_db.
  - add it to fields_sk_user in setup_static_data in sk_web and add the string
	constant for the label
  - add it in sk_user_to_fields and sk_user_from_fields
  - in fields_user_lock, lock the fields the user should not have access to.
  - in handler_user_do_edit, make sure only the sk_admin can edit the field, if
	appropriate (both for creating and editing).




